console.log("page loaded...")
function play(element) {
    element.videopreview("play")
    var video = document.play("play");
        vid.muted= true;
}
function out(element) {
    element.videopreview("pause")
    element.currentTime = 0;
}

